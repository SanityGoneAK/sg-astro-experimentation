import MapCharacterSearch from "./MapCharacterSearch";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapCharacterSearch,
} as ComponentMeta<typeof MapCharacterSearch>;

const Template: ComponentStoryObj<typeof MapCharacterSearch> = {
};

export const Default = {
  ...Template,
};
