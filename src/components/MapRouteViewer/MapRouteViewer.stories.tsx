import MapRouteViewer from "./MapRouteViewer";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapRouteViewer,
} as ComponentMeta<typeof MapRouteViewer>;

const Template: ComponentStoryObj<typeof MapRouteViewer> = {
};

export const Default = {
  ...Template,
};
