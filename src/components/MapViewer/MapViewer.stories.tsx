import MapViewer from "./MapViewer";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapViewer,
} as ComponentMeta<typeof MapViewer>;

const Template: ComponentStoryObj<typeof MapViewer> = {
};

export const Default = {
  ...Template,
};
