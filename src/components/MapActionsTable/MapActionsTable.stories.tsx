import MapActionsTable from "./MapActionsTable";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapActionsTable,
} as ComponentMeta<typeof MapActionsTable>;

const Template: ComponentStoryObj<typeof MapActionsTable> = {
};

export const Default = {
  ...Template,
};
