import MapCharacterRange from "./MapCharacterRange";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapCharacterRange,
} as ComponentMeta<typeof MapCharacterRange>;

const Template: ComponentStoryObj<typeof MapCharacterRange> = {
};

export const Default = {
  ...Template,
};
