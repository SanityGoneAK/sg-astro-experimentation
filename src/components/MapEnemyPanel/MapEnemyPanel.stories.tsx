import MapEnemyPanel from "./MapEnemyPanel";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapEnemyPanel,
} as ComponentMeta<typeof MapEnemyPanel>;

const Template: ComponentStoryObj<typeof MapEnemyPanel> = {
};

export const Default = {
  ...Template,
};
