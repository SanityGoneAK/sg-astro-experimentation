import MapToken from "./MapToken";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapToken,
} as ComponentMeta<typeof MapToken>;

const Template: ComponentStoryObj<typeof MapToken> = {
};

export const Default = {
  ...Template,
};
