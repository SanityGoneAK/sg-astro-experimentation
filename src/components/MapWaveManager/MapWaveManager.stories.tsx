import MapWaveManager from "./MapWaveManager";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapWaveManager,
} as ComponentMeta<typeof MapWaveManager>;

const Template: ComponentStoryObj<typeof MapWaveManager> = {
};

export const Default = {
  ...Template,
};
