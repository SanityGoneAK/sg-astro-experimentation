import MapWaveDetails from "./MapWaveDetails";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapWaveDetails,
} as ComponentMeta<typeof MapWaveDetails>;

const Template: ComponentStoryObj<typeof MapWaveDetails> = {
};

export const Default = {
  ...Template,
};
