import MapTile from "./MapTile";

import type { ComponentMeta, ComponentStoryObj } from "@storybook/react";

export default {
  component: MapTile,
} as ComponentMeta<typeof MapTile>;

const Template: ComponentStoryObj<typeof MapTile> = {
};

export const Default = {
  ...Template,
};
